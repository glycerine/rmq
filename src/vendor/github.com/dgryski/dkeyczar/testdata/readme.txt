    - exisiting-data -- contains test data used by unit tests in particular implementations
    - interop-data -- contains testdata produced by identical parameters with different versions of keyczar
    - special-case contains special case data that would be difficult to produce to test against

